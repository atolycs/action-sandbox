{
  "name": "action-sandbox",
  "version": "1.0.0",
  "description": "Github Action SandBox",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint:html": "npx eslint ./src/**/*.html",
    "lint:html:dog": "npx eslint ./src/**/*.html --format",
    "lint:js": "npx eslint ./src/**/*.js",
    "lint:css": "npx stylelint ./src/**/*.css",
    "lint:fix": "npx eslint ./src/**/*.{html,js} --fix && npx stylelint ./src/**/*.css --fix && npx prettier ./src/** --write"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@html-eslint/eslint-plugin": "^0.23.0",
    "@html-eslint/parser": "^0.23.0",
    "eslint": "^8.56.0",
    "eslint-config-prettier": "^9.1.0",
    "prettier": "^3.2.4",
    "stylelint": "^16.2.0",
    "stylelint-config-standard": "^36.0.0"
  }
}
